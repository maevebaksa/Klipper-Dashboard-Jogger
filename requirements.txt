# GTK and Cairo come from apt; the venv uses --system-site-packages.
requests==2.32.5
websocket-client==1.9.0
Jinja2==3.1.6
psutil==7.2.2
python-mpv==1.0.8
# 0.14.1's ARM64 wheel failed to import on Raspberry Pi OS Trixie / Python 3.13.
# 0.14.2 publishes a CPython 3.9+ abi3 ARM64 wheel with the required sd-bus bits bundled.
sdbus==0.14.2
sdbus-networkmanager==2.0.0
pygame==2.6.1
zeroconf==0.148.0
